<% provide( :title, "All Users" ) %>

<h1>
	All Users
</h1>
<%= will_paginate(  ) %>
<ul class="users">
	/* <% @users.each do | user | %>
		<%= render( user ) %>
	<% end %> */
	<%= render( @users ) %> /* Rails iterates automatically, using the _user partial. */
</ul>
<%= will_paginate(  ) %>